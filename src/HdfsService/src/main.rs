#![feature(
    arbitrary_enum_discriminant,
    type_alias_impl_trait,
    atomic_from_mut,
    inline_const,
    const_mut_refs,
    associated_type_defaults,
    array_zip
)]
/*
存储HDFS
 */
pub fn main() {
    println!("Hello, world!");
}
